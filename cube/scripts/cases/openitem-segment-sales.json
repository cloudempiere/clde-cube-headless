{
  "name": "Openitem OpenItemsSales segment selects sales invoices only, tenant-scoped",
  "note": "Segment coverage. Segments compare the RAW column produced by the cube SQL, not the dimension, so a cube whose CASE lives in the dimension instead of the SQL has segments that silently match nothing - which is exactly what had happened to Orderfacts.Sales (0 rows vs 86,370). This cube keeps its CASE in the SQL so the segment works today; the case exists so that removing the CASE - the natural boolean cleanup, since type: boolean converts 'Y'/'N' unaided - cannot break it unnoticed. Measure-level cases would all stay green. Here issotrx comes from the invoice (i.issotrx). The join chain is mirrored in full because c_invoicepayschedule and c_acctschema can multiply rows.",
  "cubeQuery": {
    "measures": [
      "Openitem.count"
    ],
    "segments": [
      "Openitem.OpenItemsSales"
    ],
    "timeDimensions": [
      {
        "dimension": "Openitem.dateinvoiced",
        "granularity": "month",
        "dateRange": [
          "2026-01-01",
          "2026-08-31"
        ]
      }
    ]
  },
  "referenceSql": "SELECT count(*) AS value FROM c_invoice i LEFT JOIN c_invoicepayschedule ips ON i.c_invoice_id = ips.c_invoice_id AND i.ispaid ='N' LEFT JOIN C_DunningLevel dl ON dl.C_DunningLevel_ID=i.C_DunningLevel_ID LEFT JOIN c_acctschema ac ON ac.ad_client_id = i.ad_client_id AND (ac.ad_orgonly_id = i.ad_org_id) JOIN c_doctype dt ON i.c_doctype_id = dt.c_doctype_id JOIN c_paymentterm p ON i.c_paymentterm_id = p.c_paymentterm_id WHERE i.ispaid ='N' AND i.ad_client_id=1000026 AND i.issotrx='Y' AND i.dateinvoiced >= DATE '2026-01-01' AND i.dateinvoiced < DATE '2026-09-01'",
  "tolerance": 0,
  "securityContext": {
    "ad_client_id": 1000026,
    "ad_language": "sk_SK",
    "roles": [
      "tenant_user"
    ]
  }
}