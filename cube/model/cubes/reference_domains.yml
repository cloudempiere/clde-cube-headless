# GENERATED by scripts/gen-reference-domains.mjs - do not edit by hand.
#
# One cube per iDempiere AD_Reference lookup domain. Each carries the same
# access_policy, filtering ad_language from the security context, so a fact
# cube joining any of them sees exactly one row per code - no fan-out, one
# compiled model, one rollup set, and adding a language is an INSERT.
#
# Replaces 24 inline rv_ad_reference_trl joins that hardcoded a language.

cubes:
  # ad_reference_id 151 - used by Orderfacts, Quotefacts
  - name: DeliveryRule
    title: Delivery Rule
    sql_alias: dr151
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 151
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Delivery Rule
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: &lang_policy
      - group: tenant_user
        member_level: { includes: "*" }
        row_level:
          filters:
            - member: ad_language
              operator: equals
              values: ["{ securityContext.ad_language }"]

  # ad_reference_id 150 - used by Orderfacts, Quotefacts
  - name: InvoiceRule
    title: Invoice Rule
    sql_alias: ir150
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 150
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Invoice Rule
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 1000116 - used by Orderfacts, Quotefacts
  - name: OrderLineStatus
    title: Order Line Status
    sql_alias: ols1000116
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 1000116
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Order Line Status
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 1000188 - used by Orderfacts, Quotefacts
  - name: LostSalesReason
    title: Lost Sales Reason
    sql_alias: lsr1000188
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 1000188
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Lost Sales Reason
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 131 - used by Warehouse (5), Logisticfacts
  - name: DocumentStatus
    title: Document Status
    sql_alias: ds131
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 131
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Document Status
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 183 - used by Warehouse (5), Logisticfacts
  - name: DocBaseType
    title: Document Base Type
    sql_alias: dbt183
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 183
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Document Base Type
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 152 - used by Logisticfacts
  - name: DeliveryViaRule
    title: Delivery Via Rule
    sql_alias: dvr152
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 152
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Delivery Via Rule
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 117 - used by Factacct, Cashflowplan (2)
  - name: AccountType
    title: Account Type
    sql_alias: at117
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 117
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Account Type
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 216 - used by Bankaccount
  - name: BankAccountType
    title: Bank Account Type
    sql_alias: bat216
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 216
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Bank Account Type
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 53385 - used by Cashflowplan
  - name: CashFlowType
    title: Cash Flow Type
    sql_alias: cft53385
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 53385
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Cash Flow Type
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy

  # ad_reference_id 1000388 - used by Openitem
  - name: OpenItemAging
    title: Open Item Aging
    sql_alias: oia1000388
    sql: >
      SELECT rl.ad_ref_list_id, rl.value, rl.name AS name_base,
             t.ad_language, t.name AS name
      FROM ad_ref_list rl
      LEFT JOIN ad_ref_list_trl t
             ON t.ad_ref_list_id = rl.ad_ref_list_id AND t.isactive = 'Y'
      WHERE rl.isactive = 'Y' AND rl.ad_reference_id = 1000388
    dimensions:
      - name: id
        sql: "ad_ref_list_id || '-' || COALESCE(ad_language,'--')"
        type: string
        primary_key: true
      - name: value
        title: Code
        sql: value
        type: string
      - name: ad_language
        sql: ad_language
        type: string
        public: false
      - name: name
        title: Open Item Aging
        description: Translated label; falls back to the base record when no translation exists
        sql: "COALESCE(name, name_base)"
        type: string
    measures:
      - name: count
        type: count
    access_policy: *lang_policy
